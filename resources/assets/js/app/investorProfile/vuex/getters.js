
/*
export const user = (state) => {
	return state.user
}
*/
//Do I need to declare this in all my vuex folder getter files??
//no, all getters gets imported and applied to the store when newing it up (in the vuex folder index file);